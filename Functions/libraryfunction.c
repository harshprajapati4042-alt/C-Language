#include<stdio.h>
#include<math.h>

int main(){
    int a,root,power;
    printf("Enter the number: ");
    scanf("%d",&a);
    root = sqrt(a);
    printf("%d\n",root);
    power = pow(2,6);
    printf("%d",power);
    return 0;
}